<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="post">

	
	<!-- 메인 피드 전체 게시글 수 -->
	<select id="totalCountMain" parameterType="int" resultType="com.sns.feed.db.FeedVO">
		SELECT *
         FROM (SELECT DISTINCT FD.F_IDX, FD.REGDATE, FD.CONTENT, FD.U_IDX, FD.U_LIKE, FD.F_PIC
				FROM FEED FD, (SELECT * FROM FOLLOW WHERE FROM_IDX = #{u_idx}) FW
				WHERE FD.U_IDX = FW.TO_IDX
				OR FD.U_IDX = #{u_idx})
        ORDER BY F_IDX DESC
	</select>
	
	<!-- 메인 피드 : 한 페이지에서 보이는 게시글 목록 -->
	<select id="mainFeed" parameterType="map" resultType="com.sns.feed.db.MainFeedVO">
		SELECT *
		FROM (SELECT ROWNUM, RR.*
		    FROM (SELECT DISTINCT FD.F_IDX, FD.REGDATE, FD.CONTENT, FD.U_IDX, FD.U_LIKE, FD.F_PIC
		        FROM FEED FD, (SELECT * FROM FOLLOW WHERE FROM_IDX = #{u_idx}) FW
		        WHERE FD.U_IDX = FW.TO_IDX
		        OR FD.U_IDX = #{u_idx}
		        ORDER BY F_IDX DESC) RR)
	    WHERE ROWNUM BETWEEN #{begin} AND #{end}
	</select>
	
	<!-- 게시글 등록 -->
	<insert id="insert" parameterType="com.sns.feed.db.FeedVO">
		INSERT INTO FEED (F_IDX, REGDATE, CONTENT, U_IDX, U_LIKE, F_PIC)
            	VALUES(FEED_SEQ.nextval, SYSDATE, #{content}, #{u_idx},0, #{f_pic})
	</insert>
	
	<!-- 로그인 -->
	<select id="login" parameterType="map" resultType="com.sns.user.db.UserVO">
		SELECT * 
		FROM USER_INFO
		WHERE U_ID = #{u_id} AND PWD = #{pwd}
	</select>

</mapper>



















